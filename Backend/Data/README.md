# Data

This folder contains the shared `ApplicationDbContext`, EF Core entity
configurations, migrations and seed data. It is intentionally shared because
this beginner-friendly modular monolith uses one database and one DbContext.
