import { Component } from '@angular/core';import { FormsModule } from '@angular/forms';import { RouterLink } from '@angular/router';import { AuthService } from '../../../../core/auth/services/auth.service';import { ApiErrorService } from '../../../../core/http/api-error.service';import { authStyles } from '../../styles/auth-layout.styles';
@Component({selector:'app-forgot-password',standalone:true,imports:[FormsModule,RouterLink],template:`<div class="auth-page"><section class="auth-art"><div class="auth-brand"><div class="auth-logo">C</div><div><strong>Campus Services</strong><span>Account recovery</span></div></div><div class="auth-copy"><h1>Recover access securely.</h1><p>Enter your account email. If the backend email service is configured, it will send the reset instructions.</p></div><div></div></section><section class="auth-main"><form class="auth-card" #f="ngForm" (ngSubmit)="submit()"><h2>Forgot password</h2><p>We'll request a password-reset token from the portal API.</p>@if(message){<div class="alert success" style="margin-bottom:16px">{{message}}</div>}@if(error){<div class="alert error" style="margin-bottom:16px">{{error}}</div>}<div class="stack"><div class="field"><label>Email address</label><input class="input" type="email" email required name="email" [(ngModel)]="email"></div><button class="btn btn-primary" [disabled]="f.invalid||loading">{{loading?'Sending…':'Send reset instructions'}}</button></div><div class="auth-footer"><a class="auth-link" routerLink="/login">Back to sign in</a></div></form></section></div>`,styles:[authStyles]})
export class ForgotPasswordComponent{email='';loading=false;message='';error='';constructor(private auth:AuthService,private errors:ApiErrorService){}submit(){this.loading=true;this.message='';this.error='';this.auth.forgotPassword(this.email).subscribe({next:()=>{this.loading=false;this.message='If the email exists, reset instructions have been requested.'},error:e=>{this.loading=false;this.error=this.errors.message(e)}})}}
