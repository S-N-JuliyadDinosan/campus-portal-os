import { Component } from '@angular/core';import { FormsModule } from '@angular/forms';import { ActivatedRoute, Router, RouterLink } from '@angular/router';import { AuthService } from '../../core/services/auth.service';import { ApiErrorService } from '../../core/services/api-error.service';import { ToastService } from '../../core/services/toast.service';import { authStyles } from './auth-layout.styles';
@Component({selector:'app-reset-password',standalone:true,imports:[FormsModule,RouterLink],template:`<div class="auth-page"><section class="auth-art"><div class="auth-brand"><div class="auth-logo">C</div><div><strong>Campus Services</strong><span>Secure reset</span></div></div><div class="auth-copy"><h1>Choose a fresh password.</h1><p>Use the reset token supplied by the backend and set a password with at least eight characters.</p></div><div></div></section><section class="auth-main"><form class="auth-card" #f="ngForm" (ngSubmit)="submit()"><h2>Reset password</h2><p>Complete both fields to continue.</p>@if(error){<div class="alert error" style="margin-bottom:16px">{{error}}</div>}<div class="stack"><div class="field"><label>Reset token</label><input class="input mono" required name="token" [(ngModel)]="token"></div><div class="field"><label>New password</label><input class="input" type="password" minlength="8" required name="password" [(ngModel)]="password"></div><button class="btn btn-primary" [disabled]="f.invalid||loading">{{loading?'Resetting…':'Reset password'}}</button></div><div class="auth-footer"><a class="auth-link" routerLink="/login">Back to sign in</a></div></form></section></div>`,styles:[authStyles]})
export class ResetPasswordComponent{token='';password='';loading=false;error='';constructor(route:ActivatedRoute,private auth:AuthService,private errors:ApiErrorService,private toast:ToastService,private router:Router){this.token=route.snapshot.queryParamMap.get('token')??''}submit(){this.loading=true;this.error='';this.auth.resetPassword(this.token,this.password).subscribe({next:()=>{this.loading=false;this.toast.success('Password reset successfully.');void this.router.navigate(['/login']);},error:e=>{this.loading=false;this.error=this.errors.message(e)}})}}
