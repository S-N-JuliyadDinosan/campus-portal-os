import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { SettingsService } from '../../core/services/portal-services';import { SystemSetting } from '../../core/models/domain.models';import { ApiErrorService } from '../../core/services/api-error.service';import { ToastService } from '../../core/services/toast.service';
@Component({selector:'app-settings-admin',standalone:true,imports:[CommonModule,FormsModule],template:`<div class="page"><div class="page-head"><div><h1>System settings</h1><p>Configure operational settings exposed by the backend administration API.</p></div></div><div class="grid grid-3"><div class="card"><h3 class="card-title">Reservation hold</h3><div class="card-sub">Controls how long held event/lab reservations remain before expiry.</div><form class="stack" #hf="ngForm" (ngSubmit)="saveHold()" style="margin-top:18px"><div class="field"><label>Hold duration (minutes)</label><input class="input" type="number" min="1" max="1440" required name="minutes" [(ngModel)]="holdMinutes"></div><button class="btn btn-primary" [disabled]="hf.invalid">Save hold duration</button></form></div><div class="card" style="grid-column:span 2"><div class="row between"><div><h3 class="card-title">All settings</h3><div class="card-sub">Key/value settings stored by the API.</div></div><button class="btn btn-secondary btn-sm" (click)="load()">Refresh</button></div><div class="divider"></div><div class="stack">@for(s of settings;track s.systemSettingId){<div class="card flat" style="background:#f8fafc"><div class="row between mobile-stack"><div><strong class="mono">{{s.settingKey}}</strong><div class="small muted">{{s.description||'No description'}} @if(s.updatedAt){<span> · Updated {{s.updatedAt|date:'medium'}}</span>}</div></div><div class="row mobile-stack"><input class="input" style="min-width:220px" [(ngModel)]="values[s.settingKey]" [name]="'v'+s.systemSettingId"><button class="btn btn-primary btn-sm" (click)="save(s)">Save</button></div></div></div>}@if(!settings.length){<div class="empty">No settings returned by the API.</div>}</div></div></div></div>`})
export class SettingsAdminComponent implements OnInit{settings:SystemSetting[]=[];values:Record<string,string>={};holdMinutes=10;constructor(private service:SettingsService,private errors:ApiErrorService,private toast:ToastService){}ngOnInit(){this.load();this.service.holdMinutes().subscribe({next:x=>this.holdMinutes=x.minutes,error:()=>{}})}load(){this.service.all().subscribe({next:x=>{this.settings=x;for(const s of x)this.values[s.settingKey]=s.settingValue},error:e=>this.toast.error(this.errors.message(e))})}save(s:SystemSetting){this.service.update(s.settingKey,this.values[s.settingKey]??'').subscribe({next:()=>{this.toast.success(`${s.settingKey} updated.`);this.load()},error:e=>this.toast.error(this.errors.message(e))})}saveHold(){this.service.updateHold(this.holdMinutes).subscribe({next:x=>{this.holdMinutes=x.minutes;this.toast.success('Reservation hold duration updated.')},error:e=>this.toast.error(this.errors.message(e))})}}
