import { Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule } from '@angular/forms';import { CertificateService } from '../../core/services/portal-services';import { CertificateRequest, CertificateType } from '../../core/models/domain.models';import { StatusBadgeComponent } from '../../shared/components/status-badge.component';import { ApiErrorService } from '../../core/services/api-error.service';import { ToastService } from '../../core/services/toast.service';
@Component({selector:'app-certificates-admin',standalone:true,imports:[CommonModule,FormsModule,StatusBadgeComponent],template:`<div class="page"><div class="page-head"><div><h1>Certificate administration</h1><p>Review document requests, update collection workflow and maintain certificate types.</p></div></div><div class="tabs"><button class="tab" [class.active]="tab==='requests'" (click)="tab='requests';loadRequests()">Requests</button><button class="tab" [class.active]="tab==='types'" (click)="tab='types'">Certificate types</button></div>@if(tab==='requests'){<div class="card"><div class="toolbar"><div class="field"><label>Status</label><select class="select" [(ngModel)]="statusFilter"><option value="">All</option>@for(s of statuses;track s){<option [value]="s">{{label(s)}}</option>}</select></div><button class="btn btn-primary" (click)="loadRequests()">Filter</button></div><div class="table-wrap"><table class="table"><thead><tr><th>ID</th><th>Student</th><th>Certificate</th><th>Reason</th><th>Status</th><th>Requested</th><th></th></tr></thead><tbody>@for(r of requests;track r.certificateRequestId){<tr><td>#{{r.certificateRequestId}}</td><td>{{r.studentName}}<div class="small muted">#{{r.studentId}}</div></td><td><strong>{{r.certificateTypeName}}</strong></td><td>{{r.reason||'—'}}</td><td><app-status-badge [value]="r.status"/></td><td>{{r.requestedAt|date:'mediumDate'}}</td><td class="right"><button class="btn btn-secondary btn-sm" (click)="editRequest(r)">Review</button></td></tr>}</tbody></table></div></div>}@if(tab==='types'){<div class="grid grid-3"><div class="card"><h3 class="card-title">{{typeEditId?'Edit':'Create'}} certificate type</h3><form class="stack" #tf="ngForm" (ngSubmit)="saveType()" style="margin-top:16px"><div class="field"><label>Name</label><input class="input" required maxlength="150" name="name" [(ngModel)]="typeForm.name"></div><div class="field"><label>Description</label><textarea class="textarea" maxlength="500" name="desc" [(ngModel)]="typeForm.description"></textarea></div>@if(typeEditId){<label class="row"><input type="checkbox" name="active" [(ngModel)]="typeForm.isActive"> Active</label>}<div class="row"><button class="btn btn-primary" [disabled]="tf.invalid">Save</button>@if(typeEditId){<button type="button" class="btn btn-secondary" (click)="resetType()">Cancel</button>}</div></form></div><div class="card" style="grid-column:span 2"><h3 class="card-title">Certificate types</h3><div class="divider"></div><div class="table-wrap"><table class="table"><thead><tr><th>Name</th><th>Description</th><th>Status</th><th></th></tr></thead><tbody>@for(t of types;track t.certificateTypeId){<tr><td><strong>{{t.name}}</strong></td><td>{{t.description||'—'}}</td><td><app-status-badge [value]="t.isActive?'Active':'Inactive'"/></td><td class="right"><div class="row" style="justify-content:flex-end"><button class="btn btn-secondary btn-sm" (click)="editType(t)">Edit</button><button class="btn btn-danger btn-sm" (click)="deleteType(t)">Deactivate</button></div></td></tr>}</tbody></table></div></div></div>}@if(selected){<div class="modal-backdrop" (click)="selected=null"><div class="modal" (click)="$event.stopPropagation()"><div class="modal-head"><div><h2>Review request #{{selected.certificateRequestId}}</h2><div class="small muted">{{selected.studentName}} · {{selected.certificateTypeName}}</div></div><button class="icon-btn" (click)="selected=null">×</button></div><form class="stack" #rf="ngForm" (ngSubmit)="saveRequest()"><div class="field"><label>Status</label><select class="select" required name="status" [(ngModel)]="requestStatus">@for(s of statuses;track s){<option [value]="s">{{label(s)}}</option>}</select></div><div class="field"><label>Review note</label><textarea class="textarea" name="note" [(ngModel)]="reviewNote"></textarea></div><div class="row"><button class="btn btn-primary" [disabled]="rf.invalid">Update request</button><button type="button" class="btn btn-secondary" (click)="selected=null">Cancel</button></div></form></div></div>}</div>`})
export class CertificatesAdminComponent implements OnInit{tab:'requests'|'types'='requests';statuses=['Pending','Approved','Rejected','ReadyForCollection','Collected'];requests:CertificateRequest[]=[];types:CertificateType[]=[];statusFilter='';selected:CertificateRequest|null=null;requestStatus='Pending';reviewNote='';typeEditId:number|null=null;typeForm:any={name:'',description:'',isActive:true};constructor(private service:CertificateService,private errors:ApiErrorService,private toast:ToastService){}ngOnInit(){this.loadTypes();this.loadRequests()}label(s:string){return s.replace(/([a-z])([A-Z])/g,'$1 $2')}loadRequests(){this.service.all(this.statusFilter||undefined).subscribe({next:x=>this.requests=x,error:e=>this.toast.error(this.errors.message(e))})}editRequest(r:CertificateRequest){this.selected=r;this.requestStatus=r.status;this.reviewNote=r.reviewNote||''}saveRequest(){if(!this.selected)return;this.service.updateStatus(this.selected.certificateRequestId,this.requestStatus,this.reviewNote||undefined).subscribe({next:()=>{this.selected=null;this.toast.success('Certificate request updated.');this.loadRequests()},error:e=>this.toast.error(this.errors.message(e))})}loadTypes(){this.service.types().subscribe({next:x=>this.types=x,error:e=>this.toast.error(this.errors.message(e))})}editType(t:CertificateType){this.typeEditId=t.certificateTypeId;this.typeForm={name:t.name,description:t.description||'',isActive:t.isActive}}resetType(){this.typeEditId=null;this.typeForm={name:'',description:'',isActive:true}}saveType(){const req=this.typeEditId?this.service.updateType(this.typeEditId,this.typeForm):this.service.createType({name:this.typeForm.name,description:this.typeForm.description||null});req.subscribe({next:()=>{this.toast.success('Certificate type saved.');this.resetType();this.loadTypes()},error:e=>this.toast.error(this.errors.message(e))})}deleteType(t:CertificateType){if(!confirm(`Deactivate ${t.name}?`))return;this.service.deleteType(t.certificateTypeId).subscribe({next:()=>{this.toast.success('Certificate type deactivated.');this.loadTypes()},error:e=>this.toast.error(this.errors.message(e))})}}
