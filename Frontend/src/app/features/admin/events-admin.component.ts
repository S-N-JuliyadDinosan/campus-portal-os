import { ChangeDetectorRef, Component, OnInit } from '@angular/core';import { CommonModule } from '@angular/common';import { FormsModule, NgForm } from '@angular/forms';import { EventService } from '../../core/services/portal-services';import { EventItem, EventRegistration, EventSeat, Venue } from '../../core/models/domain.models';import { StatusBadgeComponent } from '../../shared/components/status-badge.component';import { ApiErrorService } from '../../core/services/api-error.service';import { ToastService } from '../../core/services/toast.service';
@Component({selector:'app-events-admin',standalone:true,imports:[CommonModule,FormsModule,StatusBadgeComponent],template:`
<div class="page"><div class="page-head"><div><h1>Event administration</h1><p>Create venues and events, configure reserved seats and review registrations.</p></div></div><div class="tabs"><button class="tab" [class.active]="tab==='events'" (click)="tab='events'">Events</button><button class="tab" [class.active]="tab==='venues'" (click)="tab='venues'">Venues</button><button class="tab" [class.active]="tab==='registrations'" (click)="tab='registrations';loadRegistrations()">Registrations</button></div>
@if(tab==='events'){<div class="row between mobile-stack" style="margin-bottom:18px"><div class="muted small">{{events.length}} events loaded</div><button class="btn btn-primary" (click)="newEvent()">Create event</button></div><div class="grid grid-3">@for(e of events;track e.eventId){<article class="card"><div class="row between"><span class="badge primary">{{e.venueName}}</span><app-status-badge [value]="e.isActive?'Active':'Inactive'"/></div><h3 style="margin-top:14px">{{e.title}}</h3><div class="small muted">{{e.startAt|date:'medium'}} → {{e.endAt|date:'shortTime'}}</div><p class="small muted">Capacity {{e.capacity}} · {{e.registeredCount}} registered · {{e.usesReservedSeating?'Reserved seating':'General admission'}}</p><div class="progress"><span [style.width.%]="e.capacity?Math.min(100,e.registeredCount/e.capacity*100):0"></span></div><div class="row wrap" style="margin-top:16px"><button class="btn btn-secondary btn-sm" (click)="editEvent(e)">Edit</button>@if(e.usesReservedSeating){<button class="btn btn-secondary btn-sm" (click)="manageSeats(e)">Seats</button>}<button class="btn btn-danger btn-sm" (click)="deleteEvent(e)" [attr.aria-label]="'Delete '+e.title">Delete</button></div></article>}</div>}
@if(tab==='venues'){<div class="grid grid-3"><div class="card"><h3 class="card-title">{{venueEditId?'Edit':'Create'}} venue</h3><form class="stack" #vf="ngForm" (ngSubmit)="saveVenue(vf)" style="margin-top:16px"><div class="field"><label>Name</label><input class="input" required name="name" [(ngModel)]="venueForm.name"></div><div class="field"><label>Type</label><select class="select" required name="type" [(ngModel)]="venueForm.venueType"><option [ngValue]="1">Event hall</option><option [ngValue]="2">Open space</option></select></div><div class="field"><label>Location</label><input class="input" required name="location" [(ngModel)]="venueForm.location"></div><div class="field"><label>Capacity</label><input class="input" type="number" min="1" required name="capacity" [(ngModel)]="venueForm.capacity"></div>@if(venueEditId){<label class="row"><input type="checkbox" name="active" [(ngModel)]="venueForm.isActive"> Active</label>}<div class="row"><button class="btn btn-primary" [disabled]="vf.invalid||savingVenue">{{savingVenue?'Saving…':'Save'}}</button>@if(venueEditId){<button type="button" class="btn btn-secondary" [disabled]="savingVenue" (click)="resetVenue(vf)">Cancel</button>}</div></form></div><div class="card" style="grid-column:span 2"><h3 class="card-title">Venues</h3><div class="divider"></div><div class="table-wrap"><table class="table"><thead><tr><th>Name</th><th>Type</th><th>Location</th><th>Capacity</th><th>Status</th><th></th></tr></thead><tbody>@for(v of venues;track v.venueId){<tr><td><strong>{{v.name}}</strong></td><td>{{v.venueType===1?'Event hall':'Open space'}}</td><td>{{v.location}}</td><td>{{v.capacity}}</td><td><app-status-badge [value]="v.isActive?'Active':'Inactive'"/></td><td class="right"><div class="row" style="justify-content:flex-end"><button class="btn btn-secondary btn-sm" (click)="editVenue(v)">Edit</button><button class="btn btn-danger btn-sm" (click)="deleteVenue(v)" [attr.aria-label]="'Delete '+v.name">Delete</button></div></td></tr>}</tbody></table></div></div></div>}
@if(tab==='registrations'){<div class="card"><div class="row between"><div><h3 class="card-title">Event registrations</h3><div class="card-sub">Held, confirmed, cancelled and expired registrations.</div></div><button class="btn btn-secondary btn-sm" (click)="loadRegistrations()">Refresh</button></div><div class="divider"></div><div class="table-wrap"><table class="table"><thead><tr><th>ID</th><th>Event</th><th>Student</th><th>Seat</th><th>Status</th><th>Registered</th></tr></thead><tbody>@for(r of registrations;track r.eventRegistrationId){<tr><td>#{{r.eventRegistrationId}}</td><td>{{r.eventTitle}}</td><td>{{r.studentName||('#'+r.studentId)}}</td><td>{{r.seatNumber||'General'}}</td><td><app-status-badge [value]="regStatus(r.status)"/></td><td>{{r.registeredAt|date:'medium'}}</td></tr>}</tbody></table></div></div>}
@if(eventModal){<div class="modal-backdrop" (click)="eventModal=false"><div class="modal" (click)="$event.stopPropagation()"><div class="modal-head"><h2>{{eventEditId?'Edit':'Create'}} event</h2><button class="icon-btn" (click)="eventModal=false">×</button></div><form class="form-grid" #ef="ngForm" (ngSubmit)="saveEvent()"><div class="field"><label>Title</label><input class="input" required name="title" [(ngModel)]="eventForm.title"></div><div class="field"><label>Venue</label><select class="select" required name="venue" [(ngModel)]="eventForm.venueId">@for(v of venues;track v.venueId){<option [ngValue]="v.venueId">{{v.name}}</option>}</select></div><div class="field"><label>Starts</label><input class="input" type="datetime-local" required name="start" [(ngModel)]="eventForm.startAt"></div><div class="field"><label>Ends</label><input class="input" type="datetime-local" required name="end" [(ngModel)]="eventForm.endAt"></div><div class="field"><label>Capacity</label><input class="input" type="number" min="1" required name="capacity" [(ngModel)]="eventForm.capacity"></div><div class="field"><label class="row" style="margin-top:28px"><input type="checkbox" name="reserved" [(ngModel)]="eventForm.usesReservedSeating"> Reserved seating</label></div><div class="field full"><label>Description</label><textarea class="textarea" name="desc" [(ngModel)]="eventForm.description"></textarea></div><label class="row"><input type="checkbox" name="published" [(ngModel)]="eventForm.isPublished"> Published</label>@if(eventEditId){<label class="row"><input type="checkbox" name="active" [(ngModel)]="eventForm.isActive"> Active</label>}<div class="row"><button class="btn btn-primary" [disabled]="ef.invalid||savingEvent">{{savingEvent?'Saving…':'Save event'}}</button><button type="button" class="btn btn-secondary" [disabled]="savingEvent" (click)="eventModal=false">Cancel</button></div></form></div></div>}
@if(seatEvent){<div class="modal-backdrop" (click)="seatEvent=null"><div class="modal" (click)="$event.stopPropagation()"><div class="modal-head"><div><h2>Seats · {{seatEvent.title}}</h2><div class="small muted">Create or deactivate seat identifiers.</div></div><button class="icon-btn" (click)="seatEvent=null">×</button></div><form class="row mobile-stack" #sf="ngForm" (ngSubmit)="createSeat()"><input class="input grow" required name="seatNo" [(ngModel)]="newSeatNumber" placeholder="Seat number"><input class="input grow" name="section" [(ngModel)]="newSeatSection" placeholder="Section (optional)"><button class="btn btn-primary" [disabled]="sf.invalid">Add</button></form><div class="divider"></div><div class="row wrap">@for(s of seats;track s.eventSeatId){<div class="row" style="padding:8px 10px;background:#f8fafc;border-radius:10px"><strong>{{s.seatNumber}}</strong>@if(s.sectionName){<span class="small muted">{{s.sectionName}}</span>}<app-status-badge [value]="s.isActive?'Active':'Inactive'"/><button class="btn btn-danger btn-sm" (click)="deleteSeat(s)">×</button></div>}</div></div></div>}
</div>`})
export class EventsAdminComponent implements OnInit{tab:'events'|'venues'|'registrations'='events';events:EventItem[]=[];venues:Venue[]=[];registrations:EventRegistration[]=[];Math=Math;eventModal=false;eventEditId:number|null=null;eventForm:any={};savingEvent=false;venueEditId:number|null=null;savingVenue=false;venueForm:any={name:'',venueType:1,location:'',capacity:100,isActive:true};seatEvent:EventItem|null=null;seats:EventSeat[]=[];newSeatNumber='';newSeatSection='';constructor(private service:EventService,private errors:ApiErrorService,private toast:ToastService,private cdr:ChangeDetectorRef){}ngOnInit(){this.loadVenues();this.loadEvents()}loadEvents(){this.service.events({pageSize:100}).subscribe({next:x=>{this.events=x;this.cdr.markForCheck()},error:e=>this.toast.error(this.errors.message(e))})}loadVenues(){this.service.venues().subscribe({next:x=>{this.venues=this.sortVenues(x);this.cdr.markForCheck()},error:e=>this.toast.error(this.errors.message(e))})}newEvent(){this.eventEditId=null;const d=new Date(Date.now()+86400000);const end=new Date(d.getTime()+7200000);this.eventForm={venueId:this.venues[0]?.venueId||null,title:'',description:'',startAt:this.local(d),endAt:this.local(end),capacity:100,usesReservedSeating:false,isPublished:true,isActive:true};this.eventModal=true}editEvent(e:EventItem){this.eventEditId=e.eventId;this.eventForm={venueId:e.venueId,title:e.title,description:e.description||'',startAt:this.local(new Date(e.startAt)),endAt:this.local(new Date(e.endAt)),capacity:e.capacity,usesReservedSeating:e.usesReservedSeating,isPublished:e.isPublished,isActive:e.isActive};this.eventModal=true}saveEvent(){if(this.savingEvent)return;const b={...this.eventForm,startAt:new Date(this.eventForm.startAt).toISOString(),endAt:new Date(this.eventForm.endAt).toISOString(),description:this.eventForm.description||null};this.savingEvent=true;if(this.eventEditId!==null){this.service.updateEvent(this.eventEditId,b).subscribe({next:()=>this.finishEventSave(),error:e=>this.failEventSave(e)});return}this.service.createEvent(b).subscribe({next:created=>{this.upsertEvent(created);this.finishEventSave()},error:e=>this.failEventSave(e)})}private finishEventSave(){this.savingEvent=false;this.eventModal=false;this.toast.success('Event saved.');this.loadEvents();this.cdr.markForCheck()}private failEventSave(error:unknown){this.savingEvent=false;this.toast.error(this.errors.message(error));this.cdr.markForCheck()}private upsertEvent(event:EventItem){this.events=[...this.events.filter(item=>item.eventId!==event.eventId),event].sort((left,right)=>new Date(left.startAt).getTime()-new Date(right.startAt).getTime());this.cdr.markForCheck()}deleteEvent(e:EventItem){if(!confirm(`Delete ${e.title}? This also removes its registrations and seats.`))return;this.service.deleteEvent(e.eventId).subscribe({next:()=>{this.events=this.events.filter(item=>item.eventId!==e.eventId);this.toast.success('Event deleted.');this.cdr.markForCheck()},error:er=>this.toast.error(this.errors.message(er))})}editVenue(v:Venue){this.venueEditId=v.venueId;this.venueForm={name:v.name,venueType:v.venueType,location:v.location,capacity:v.capacity,isActive:v.isActive}}resetVenue(form?:NgForm){this.venueEditId=null;this.venueForm={name:'',venueType:1,location:'',capacity:100,isActive:true};form?.resetForm(this.venueForm)}saveVenue(form:NgForm){if(form.invalid||this.savingVenue)return;const editId=this.venueEditId;const payload={name:String(this.venueForm.name).trim(),venueType:this.venueForm.venueType,location:String(this.venueForm.location).trim(),capacity:this.venueForm.capacity,...(editId!==null?{isActive:this.venueForm.isActive}:{})};this.savingVenue=true;const req=editId!==null?this.service.updateVenue(editId,payload):this.service.createVenue(payload);req.subscribe({next:saved=>{if(editId!==null){this.venues=this.sortVenues(this.venues.map(v=>v.venueId===editId?{...v,...payload}:v).filter(v=>v.isActive))}else{const created=saved as Venue;this.venues=this.sortVenues([...this.venues.filter(v=>v.venueId!==created.venueId),created])}this.savingVenue=false;this.resetVenue(form);this.toast.success('Venue saved.');this.cdr.markForCheck()},error:e=>{this.savingVenue=false;this.toast.error(this.errors.message(e));this.cdr.markForCheck()}})}deleteVenue(v:Venue){if(!confirm(`Delete ${v.name} from the venue list?`))return;this.service.deleteVenue(v.venueId).subscribe({next:()=>{this.venues=this.venues.filter(item=>item.venueId!==v.venueId);this.toast.success('Venue deleted.');this.cdr.markForCheck()},error:e=>this.toast.error(this.errors.message(e))})}private sortVenues(venues:Venue[]){return [...venues].sort((left,right)=>left.name.localeCompare(right.name))}manageSeats(e:EventItem){this.seatEvent=e;this.loadSeats()}loadSeats(){if(!this.seatEvent)return;this.service.seats(this.seatEvent.eventId).subscribe({next:x=>this.seats=x,error:e=>this.toast.error(this.errors.message(e))})}createSeat(){if(!this.seatEvent)return;this.service.createSeat(this.seatEvent.eventId,{seatNumber:this.newSeatNumber,sectionName:this.newSeatSection||null,rowLabel:null}).subscribe({next:()=>{this.newSeatNumber='';this.newSeatSection='';this.toast.success('Seat created.');this.loadSeats()},error:e=>this.toast.error(this.errors.message(e))})}deleteSeat(s:EventSeat){if(!confirm(`Deactivate seat ${s.seatNumber}?`))return;this.service.deleteSeat(s.eventSeatId).subscribe({next:()=>{this.toast.success('Seat deactivated.');this.loadSeats()},error:e=>this.toast.error(this.errors.message(e))})}loadRegistrations(){this.service.registrations({pageSize:100}).subscribe({next:x=>this.registrations=x,error:e=>this.toast.error(this.errors.message(e))})}regStatus(n:number){return ({1:'Held',2:'Confirmed',3:'Cancelled',4:'Expired'} as any)[n]||n}local(d:Date){const p=(n:number)=>String(n).padStart(2,'0');return `${d.getFullYear()}-${p(d.getMonth()+1)}-${p(d.getDate())}T${p(d.getHours())}:${p(d.getMinutes())}`}}
